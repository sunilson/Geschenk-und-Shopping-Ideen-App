export class Product {
    constructor(public id: string, public added: string, public description: string, public url: string, public categoryname: string, public name: string, public sex: string, public image: string, public seller: number, public category: number, public price: number, public sellername: string) {

    }
}